<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/styles/products.css">
</head>
<body>
    
    {{>navbar}}

    <div class="product-box">
        {{#each products as |p|}}
            <div class="product-details">
                <div class="image" style="background-image: url({{p.imageURL}});">
                </div>

                <div class="info">
                    <h2 class="name">{{p.name}}</h2>
                    <h3 class="price">Rs. {{p.price}}</h3>
                    <p class="desc">{{p.desc}}</p>
                </div>

                <div class="buttons">
                    <button class="edit-btn"><a href="/admin/edit?id={{p._id}}">Edit</a></button>
                    <button class="details-btn"><a href="/admin/details?id={{p._id}}">Details</a></button>
                    <button class="delete-btn"><a href="/admin/delete?id={{p._id}}">Delete</a></button>
                </div>
            </div>
        {{/each}}
    </div>
</body>
</html>
